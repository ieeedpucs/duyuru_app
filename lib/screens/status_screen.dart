import 'package:flutter/cupertino.dart';

class StatusScreen extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Etkinlikler",style: TextStyle(fontSize: 20.0),),
    );
  }

}