import 'package:flutter/cupertino.dart';

class CallStatus extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Yönetici Paneli",style: TextStyle(fontSize: 20.0),),
    );
  }

}